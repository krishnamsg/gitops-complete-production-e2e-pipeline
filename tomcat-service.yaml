# tomcat-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: complete-production-e2e-app
spec:
  selector:
    app: tomcatcomplete-prodcution-e2e-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort

